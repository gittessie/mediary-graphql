export var colorFromIndex = function (index) {
    var idx = ((index) % 9) + 1;
    return idx <= 4 ? "accent-" + idx : "neutral-" + (idx - 4);
};
export var colorForName = function (name, theme) { return theme.global.colors[name] || name; };
