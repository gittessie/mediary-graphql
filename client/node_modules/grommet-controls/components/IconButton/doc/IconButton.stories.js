import React from 'react';
import { Box } from 'grommet';
import { Favorite } from 'grommet-icons';
import { action } from '@storybook/addon-actions';
import { IconButton } from '../IconButton';
export default {
    title: 'CONTROLS/controls/IconButton',
    component: IconButton,
};
export var main = function () { return (React.createElement(Box, { basis: 'medium', align: 'center' },
    React.createElement(IconButton, { icon: React.createElement(Favorite, null), onClick: action('Clicked') }))); };
