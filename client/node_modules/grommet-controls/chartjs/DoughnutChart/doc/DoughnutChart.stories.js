import React from 'react';
import { DoughnutChart } from '../DoughnutChart';
import { rndDatasets } from '../../data/data';
export default {
    title: 'CHARTS/chartjs/DoughnutChart',
    component: DoughnutChart,
};
export var main = function () { return (React.createElement(DoughnutChart, { data: {
        labels: ['January', 'February', 'March', 'April', 'May', 'June', 'July'],
        datasets: [
            {
                label: 'Dataset 1',
                data: [-93, 45, 35, -33, 8, 88, -62],
            },
        ],
    }, options: {
        legend: {
            display: false,
        },
        themedData: true,
    } })); };
export var animation = function () { return (React.createElement(DoughnutChart, { data: rndDatasets(1), options: {
        themedData: true,
        animation: {
            animateScale: true,
            animateRotate: true,
        },
    } })); };
