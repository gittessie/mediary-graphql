import React from 'react';
import { PieChart } from '../PieChart';
import { rndDatasets } from '../../data/data';
export default {
    title: 'CHARTS/chartjs/PieChart',
    component: PieChart,
};
export var main = function () { return (React.createElement(PieChart, { data: {
        labels: ['January', 'February', 'March', 'April', 'May', 'June', 'July'],
        datasets: [
            {
                label: 'Dataset 1',
                data: [-93, 45, 35, -33, 8, 88, -62],
            },
        ],
    }, options: {
        legend: {
            display: false,
        },
        themedData: true,
    } })); };
export var themedData = function () { return (React.createElement(PieChart, { data: rndDatasets(1), options: {
        themedData: true,
    } })); };
